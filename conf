#celery works under windows with this line
celery -A WSP worker --pool=solo -l info

#for celery to work standard config of rabbitmq is needed

python manage.py makemigrations
python manage.py migrate

install requirements by using this command:

pip install -r requirements.txt